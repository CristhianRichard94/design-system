{
  "name": "design-system",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@8.15.9",
  "devDependencies": {
    "tailwindcss": "^3.4.7",
    "typescript": "^5.5.4"
  }
}
